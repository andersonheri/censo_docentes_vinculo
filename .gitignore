# Dados brutos (grandes, de distribuição pública — não versionar)
data/raw/*
!data/raw/README.md

# IDEB: baixado automaticamente por R/05_importar_ideb.R
data/raw_ideb/

# Dados processados (gerados localmente pelo pipeline)
data/processed/*

# R
.Rhistory
.RData
.Rproj.user/
.Ruserdata

# SO
.DS_Store
Thumbs.db

# Artefatos intermediários de renderização do quarto/LaTeX (relatorios/)
# -- os deliverables finais (.html, .pdf) e o .qmd/_identificacao.tex
# fonte SÃO versionados; só o lixo de build fica de fora.
relatorios/*.aux
relatorios/*.log
relatorios/*.toc
relatorios/*.knit.md
relatorios/relatorio_final.tex
relatorios/relatorio_final_files/
